# C MQTT Sensor Agent Configuration

device_id=device-c-001
mqtt_broker_url=mqtt://localhost:1883
topic=sensors/device-c-001/metrics
interval_ms=1000
anomaly_spike_prob=0.01
# Base GPS coordinates for this device; a small random jitter is applied
# each publish so the dashboard map has lat/lng to plot.
lat=37.335
lng=-121.881

